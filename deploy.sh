#!/bin/bash
# Deploy GulfCyber.io to GitHub Pages

# 1. Create GitHub repo (manually or via gh CLI)
# gh repo create gulfcyber-site --public --source=. --push

# 2. Or deploy to Vercel
# npx vercel --prod

# 3. Or deploy to Netlify  
# npx netlify deploy --prod --dir=.

echo "Run one of the above commands to deploy"
